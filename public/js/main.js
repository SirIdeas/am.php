// Generated by CoffeeScript 1.9.1
(function() {
  $(function() {
    $('.spyscroll').each(function() {
      var eq, neq, self, strClass, target;
      self = this;
      target = $(this).attr('data-target');
      eq = eval($(this).attr('data-eq'));
      neq = eval($(this).attr('data-neq'));
      strClass = $(this).attr('data-class');
      return $(document).scroll(function() {
        var scrollValue;
        scrollValue = $(this).scrollTop();
        if ($(target).length > 0) {
          if (scrollValue >= $(target).outerHeight()) {
            return $(self).addClass(strClass);
          } else {
            return $(self).removeClass(strClass);
          }
        } else if (eq !== void 0) {
          if (scrollValue === eq) {
            return $(self).addClass(strClass);
          } else {
            return $(self).removeClass(strClass);
          }
        } else if (neq !== void 0) {
          if (scrollValue !== neq) {
            return $(self).addClass(strClass);
          } else {
            return $(self).removeClass(strClass);
          }
        }
      });
    });
    return $(document).trigger('scroll');
  });

}).call(this);
